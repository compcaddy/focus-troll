{
  "name": "extension",
  "version": "1.0.0",
  "main": "background.js",
  "scripts": {
    "build:css": "npx tailwindcss -i ./src/input.css -o ./tailwind.css --watch=false",
    "build:copy": "rm -rf builds/dev && mkdir -p builds/dev && cp manifest.json popup.html popup.js popup2.js toast.js data.js background.js tailwind.css builds/dev/ && cp -r icons builds/dev/ && cp -r images builds/dev/",
    "build": "npm run build:css && npm run build:copy",
    "build:watch": "npx tailwindcss -i ./src/input.css -o ./tailwind.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.13",
    "tailwindcss": "^4.1.13"
  }
}
